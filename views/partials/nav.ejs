<% if (nav === true) { %>
    <div class="w-full px-10 py-3 flex justify-between bg-zinc-800 text-white">
    <h3>Pintress</h3>
    <div class="flex gap-10 items-center">
        <a href="/">Profile</a>
        <a href="#">Feed</a>
        <a href="/logout" class="px-3 py-3 bg-red-700 text-white rounded-lg">Logout</a>
    </div>
</div>
<% } %>

